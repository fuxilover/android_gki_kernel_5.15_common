name: Build GKI Kernel with KSU Next, SUSFS, and KProfiles

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Kernel (arm64)
    runs-on: ubuntu-latest

    steps:
    - name: Maximize Build Space
      run: |
        sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php /usr/local/lib/android /opt/ghc
        sudo apt-get clean

    - name: Configure Git
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential flex libssl-dev libelf-dev xz-utils curl git python3 ccache

    - name: Clone GKI Source and Utilities
      run: |
        mkdir -p kernel-workspace
        cd kernel-workspace
        git clone https://github.com/fuxilover/android_gki_kernel_5.15_common.git common --depth=1
        git clone https://android.googlesource.com/kernel/build --depth=1
        git clone https://github.com/mvaisakh/gcc-arm64.git prebuilts/gcc/linux-x86/aarch64/aarch64-elf --depth=1
        git clone https://github.com/mvaisakh/gcc-arm.git prebuilts/gcc/linux-x86/arm/arm-eabi --depth=1
        git clone https://github.com/kdrag0n/proton-clang.git clang-bin --depth=1

    - name: Patch KernelSU Next, SUSFS, and KProfiles
      run: |
        cd kernel-workspace
        git clone https://github.com/WildKernels/GKI_KernelSU_SUSFS.git wild_repo --depth=1
        
        # 1. Интеграция KernelSU Next
        rm -rf common/drivers/kernelsu
        mkdir -p common/drivers/kernelsu
        
        if [ -d "wild_repo/KernelSU-Next" ]; then
            cp -r wild_repo/KernelSU-Next/* common/drivers/kernelsu/
        else
            git clone https://github.com/rifsxd/KernelSU-Next.git common/drivers/kernelsu --depth=1
        fi
        
        grep -q "kernelsu" common/drivers/Kconfig || sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' common/drivers/Kconfig
        grep -q "kernelsu" common/drivers/Makefile || echo "obj-y += drivers/kernelsu/" >> common/drivers/Makefile
        
        # 2. Интеграция SUSFS
        mkdir -p susfs_source
        git clone https://github.com/sidex15/susfs4ksu.git susfs_source --depth=1
        
        cp susfs_source/kernel_patches/fs/susfs.c common/fs/
        cp susfs_source/kernel_patches/include/linux/susfs.h common/include/linux/
        
        WILD_PATCH_515=$(find wild_repo/ -name "*5.15*.patch" | head -n 1)
        if [ -f "$WILD_PATCH_515" ]; then
            cd common && patch -p1 < "../$WILD_PATCH_515" || true && cd ..
        else
            STANDARD_515=$(find susfs_source/kernel_patches/ -name "*5.15*.patch" | head -n 1)
            cd common && patch -p1 < "../$STANDARD_515" || true && cd ..
        fi
        
        KSU_PATCH=$(find wild_repo/ -name "*ksu*.patch" -o -name "*next*.patch" | head -n 1)
        if [ -f "$KSU_PATCH" ]; then
            cd common/drivers/kernelsu && patch -p1 < "../../../$KSU_PATCH" || true && cd ../../..
        else
            STANDARD_KSU=$(find susfs_source/ksu_patches/ -name "*next*.patch" -o -name "*ksu*.patch" | head -n 1)
            cd common/drivers/kernelsu && patch -p1 < "../../../$STANDARD_KSU" || true && cd ../../..
        fi

        # 3. Проверка и интеграция KProfiles (если отсутствует в исходниках)
        if [ ! -d "common/drivers/misc/kprofiles" ] && [ ! -f "common/drivers/misc/kprofiles.c" ]; then
            echo "[+] KProfiles не найден в исходниках, интегрируем..."
            git clone https://github.com/tiann/kprofiles.git kprofiles_repo --depth=1
            mkdir -p common/drivers/misc/kprofiles
            cp -r kprofiles_repo/kernel/* common/drivers/misc/kprofiles/
            grep -q "kprofiles" common/drivers/misc/Kconfig || sed -i '/endmenu/i source "drivers/misc/kprofiles/Kconfig"' common/drivers/misc/Kconfig
            grep -q "kprofiles" common/drivers/misc/Makefile || echo "obj-y += kprofiles/" >> common/drivers/misc/Makefile
        fi

    - name: Execute GKI Build Process
      run: |
        cd kernel-workspace
        
        # Инжектируем CONFIG-флаги в gki_defconfig перед самой сборкой
        DEFCONFIG="common/arch/arm64/configs/gki_defconfig"
        echo "CONFIG_KSU=y" >> $DEFCONFIG
        echo "CONFIG_KSU_NEXT=y" >> $DEFCONFIG
        echo "CONFIG_SUSFS=y" >> $DEFCONFIG
        echo "CONFIG_KSU_SUSFS=y" >> $DEFCONFIG
        echo "CONFIG_KPROFILES=y" >> $DEFCONFIG
        
        # Настройка путей тулчейна
        export PROTON_PATH="$(pwd)/clang-bin/bin"
        export GCC64_PATH="$(pwd)/prebuilts/gcc/linux-x86/aarch64/aarch64-elf/bin"
        export GCC32_PATH="$(pwd)/prebuilts/gcc/linux-x86/arm/arm-eabi/bin"
        export PATH="$PROTON_PATH:$GCC64_PATH:$GCC32_PATH:$PATH"
        
        # Явно переопределяем переменные сборщика, чтобы build.sh не искал Clang от Google
        BUILD_CONFIG=common/build.config.gki.arm64 \
        ARCH=arm64 \
        SUBARCH=arm64 \
        CLANG_TRIPLE=aarch64-linux-gnu- \
        CROSS_COMPILE=aarch64-elf- \
        CROSS_COMPILE_COMPAT=arm-eabi- \
        LD=ld.lld \
        AR=llvm-ar \
        NM=llvm-nm \
        OBJCOPY=llvm-objcopy \
        OBJDUMP=llvm-objdump \
        STRIP=llvm-strip \
        build/build.sh

    - name: Upload Compiled Kernel Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: GKI-Kernel-Xiaomi13
        path: |
          kernel-workspace/out/android14-5.15/dist/Image
          kernel-workspace/out/android14-5.15/dist/Image.lz4
          kernel-workspace/out/android14-5.15/dist/Image.gz
